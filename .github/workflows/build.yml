name: Build ZMK firmware
on:
  push:
    tags:
      - "v*" # only run on tags like v1.0.0
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3

  release:
    if: startsWith(github.ref, 'refs/tags/')
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download firmware artifact
        uses: actions/download-artifact@v4
        with:
          name: firmware.zip
          path: ./release

      - name: Rename firmware zip with tag
        run: mv ./release/firmware.zip ./release/firmware-${{ github.ref_name }}.zip

      - name: Create GitHub Release (draft prerelease with notes)
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          files: ./release/firmware-${{ github.ref_name }}.zip
          draft: true
          prerelease: true
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
