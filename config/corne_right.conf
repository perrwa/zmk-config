# The right half has no hardware-level default forcing it central, but
# this is set explicitly (not just left implicit via the Kconfig default)
# to match the left half's *.conf and stay correct if that ever changes.
CONFIG_ZMK_SPLIT_ROLE_CENTRAL=n
