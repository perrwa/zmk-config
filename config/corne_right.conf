# corne_right has no shield-level default forcing it central, but this is
# set explicitly (not just left implicit via the Kconfig default) to match
# corne_left.conf and stay correct if ZMK ever adds one.
CONFIG_ZMK_SPLIT_ROLE_CENTRAL=n
