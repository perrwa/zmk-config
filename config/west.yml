manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    - name: perrwa
      url-base: https://github.com/perrwa
  projects:
    - name: zmk
      remote: zmkfirmware
      revision: v0.3
      import: app/west.yml
    - name: zmk-component-raytac-dongle
      remote: perrwa
      # TEMPORARY, for verifying double-press &bootloader support before
      # merging. Revert to remote: rschenk / refs/heads/v0.3 afterward.
      revision: refs/heads/perrwa/v0.3-cx40-bootloader
  self:
    path: config
